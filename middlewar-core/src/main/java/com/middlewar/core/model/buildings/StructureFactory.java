package com.middlewar.core.model.buildings;

import com.middlewar.core.model.commons.StatsSet;
import com.middlewar.core.model.items.Structure;

/**
 * @author LEBOC Philippe
 */
public final class StructureFactory extends ItemFactory<Structure> {
    public StructureFactory(StatsSet set) {
        super(set);
    }
}
